(() => {

    const setLayout = () => {
        // 배너 화면 비율 조정
        const oneRatio = innerWidth / innerHeight;
        if(innerWidth > 768) {
            // 16 : 9
        } else {
            // 1 : 0.95
        }
    }

    document.addEventListener('load', () => {

    })
})()